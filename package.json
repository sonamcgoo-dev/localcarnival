{
  "name": "localcircus",
  "version": "0.1.0",
  "description": "LocalCircus - A creative operating environment that amplifies human capability through intelligent tools, automation, and collaboration",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w @localcircus/core build && npm run -w @localcircus/storage build && npm run -w @localcircus/bigtop build && npm run -w @localcircus/provider-sdk build && npm run -w @localcircus/workflow-sdk build && npm run -w @localcircus/artifact-sdk build && npm run -w @localcircus/codex build && npm run -w @localcircus/ui build && npm run -w @localcircus/plugin-runtime build && npm run -w @localcircus/zoo build && npm run -w @localcircus/codeforge build && npm run -w @localcircus/registry build && npm run -w @localcircus/desktop build && npm run -w @localcircus/cli build",
    "build:all": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "eslint packages --ext .ts",
    "clean": "rm -rf packages/*/dist",
    "desktop": "npm run dev --workspace=@localcircus/desktop",
    "start": "npm run desktop"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "eslint": "^8.56.0",
    "@types/node": "^20.10.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/sonamcgoo-dev/localcarnival"
  },
  "keywords": [
    "localcircus",
    "artifacts",
    "workflow",
    "automation",
    "ai",
    "creative"
  ],
  "author": "LocalCircus Team",
  "license": "CC0-1.0"
}
